nerdfont: true
mode: "session"
merge_keep: false
agents:
  # `agent: omp` in any .workmux.yaml resolves to this entry, so every worktree
  # gets the 4 GB cap without editing a single project file.
  omp: "~/.local/scripts/omp-capped"
  cc-work: "claude"
  cc-personal: "env CLAUDE_CONFIG_DIR=~/.claude-personal claude"
  cod: "codex --yolo"

agent: cc-work

windows:
  - name: 1e
    panes:
      - command: nvim
        focus: true
  - name: 1a
    panes:
      - command: <agent>

  - name: 1s
  - name: 1ss
  - name: 1sss

# File operations when creating a worktree.
files:
  # Files to copy (useful for .env files that need to be unique).
  copy:
    - .env

  # Files/directories to symlink (saves disk space, shares caches).
  # Default: None.
  # Use "<global>" to inherit from global config.
  symlink:
    - .workmux.yaml
    - CLAUDE.local.md
    - .envrc
